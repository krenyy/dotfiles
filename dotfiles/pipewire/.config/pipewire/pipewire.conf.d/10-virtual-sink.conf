context.modules = [
    {   name = libpipewire-module-loopback
        args = {
            node.description = "Virtual Sink"
            capture.props = {
                node.name = "virtual-sink"
                media.class = "Audio/Sink"
                audio.position = [ FL FR ]
            }
            playback.props = {
                node.name = "playback.virtual-sink"
                audio.position = [ FL FR ]
                stream.dont-remix = true
                node.passive = true
            }
        }
    }
]
